# Nansen Flow Intelligence Configuration

# Server-side only. Used by the Vercel API route.
NANSEN_API_KEY=your_nansen_api_key

# Optional. Force server-side schema-compatible mock data.
NANSEN_MOCK_FALLBACK=0

# Optional. Force frontend-only demo mode.
VITE_NANSEN_DEMO=0
